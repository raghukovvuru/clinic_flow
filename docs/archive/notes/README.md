# Archived Notes

Archived notes are superseded handoff records. They may explain historical context, but they are not current source-of-truth docs.

For current agent context, start with `CONTEXT_INDEX.md` and use active notes in `docs/notes/` only when continuing an active feature track.
